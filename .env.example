# Database Configuration
# WARNING: Only use this default password for LOCAL DEVELOPMENT
# NEVER use this in production!
POSTGRES_PASSWORD=cookbook_dev_password

# Secret Key for Flask
# IMPORTANT: SECRET_KEY auto-generates on each startup if not set
# This invalidates all sessions on restart - set a persistent key to avoid this
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=your-generated-key-here

# For local development with Docker Compose:
# 1. Copy this file to .env: cp .env.example .env
# 2. (Recommended) Set a persistent SECRET_KEY to avoid session loss on restart:
#    python -c "import secrets; print(secrets.token_hex(32))"
# 3. Optionally change POSTGRES_PASSWORD for added security

# For production deployment:
# 1. NEVER commit .env file to version control
# 2. REQUIRED: Set a persistent SECRET_KEY to maintain sessions across restarts
#    - Without this, all users will be logged out on every restart
#    - In multi-instance deployments, all instances need the same key
# 3. Use environment-specific secret management:
#    - AWS Secrets Manager
#    - HashiCorp Vault
#    - Kubernetes Secrets
#    - GitHub Actions Secrets (for CI/CD)
# 4. Generate strong random keys:
#    SECRET_KEY: At least 32 bytes (64 hex characters)
#    POSTGRES_PASSWORD: Strong password with mixed characters
# 5. Rotate secrets regularly (with planned session invalidation)
